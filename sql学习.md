---
[[sql]] [[sql injection]]
---
数据库结构：
1. 数据库管理系统
如mysql,sql sever等，每个数据库都有不同语法，@@version能爆出版本
2. 数据库
每个服务器都有不同的数据库，
- 攻击者需要找的需要的数据库，然后实施注入，实施攻击
# 我所需要的sql语法知识
1. SELECT ... FROM(查询命令)
从数据库中拿数据的基本命令

2. WHERE(条件)(???)
给查询加条件，拿走一部分
- 攻击者需要通过注入把原本有限制的条件改编成永久条件或其他条件

3. '(单引号)
在sql中，用于包裹字符串(???)
- 攻击者通过输入单引号提前闭合原本字符串，然后在后面开始写sql代码

4. AND\OR(逻辑运算)
AND是与运算，OR是或运算
用OR来绕过认证，AND用来盲注（???）

5. --、#（注释符）
怎么使用(???)

6. UNION(联合查询)
将SELECT结果合并(???)

7. order by(排序)(???)
根据指定列对结果进行排序

8. information_schema(信息数据库)
