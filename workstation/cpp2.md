---
[[cpp]] [[cpp_operator]]
# 运算符
## 算术运算符
### 浮点运算
- 浮点运算有舍入误差 //不要随意用来==
## 逻辑运算符
### 与
- &&,同时满足
### 非!
- 相反
### 或||
- 有一个成立即可
注意：逻辑运算符有短路求值特性，有时你后面的表达式不一定会执行
# 选择与分支
//注意，尽量避免if...else...嵌套
改进方法:
1. ?:表达式
- ?:是一个运算符
语法: xxx = a ? b : c; //重点在于求值
如果a成立结果为b,反之结果为c
# rand()函数是c标准库函数
- 用法: rand() % 1000表示随机一个1000以内的数 rand() % 10表示随机一个100以内的数
